% !TeX root = ../slides.tex
% 04-environment.tex — implementation details of the custom Gymnasium env.

\section{Environment Implementation}

\begin{frame}{Custom Gymnasium Environment}
  \texttt{MinesweeperEnv(gym.Env)} wraps a pure game-logic class
  \texttt{Game}, decoupled from the Gymnasium API and from rendering:
  \begin{itemize}
    \item \texttt{Game}: mine placement, adjacency counts, flood-fill
      uncovering, win/loss detection.
    \item \texttt{MinesweeperEnv}: \texttt{reset}/\texttt{step}/
      \texttt{render}/\texttt{close}, reward, episode statistics.
    \item Optional \texttt{human} render, fully independent from the
      agent's observation.
  \end{itemize}
\end{frame}

\begin{frame}{Environment Architecture}
  \centering
  \vspace{0.4cm}
  \begin{tikzpicture}[node distance=1.1cm]
    \node[component] (game) {\texttt{Game}\\ \small board logic};
    \node[component, right=of game] (env) {\texttt{Minesweeper}\\ \texttt{Env}};
    \node[component, above=0.9cm of env] (render) {Pygame\\ \small \texttt{human} render};
    \node[component, right=of env] (agent) {Agent\\ \small policy};
    \draw[flowarrow] (game) -- (env);
    \draw[flowarrow] (env) -- (render);
    \draw[flowarrow] ([yshift=2.5mm]env.east) -- node[midway, above, font=\small] {obs, $r$} ([yshift=2.5mm]agent.west);
    \draw[flowarrow] ([yshift=-2.5mm]agent.west) -- node[midway, below, font=\small] {action} ([yshift=-2.5mm]env.east);
  \end{tikzpicture}
  \vspace{0.5cm}

  \small \texttt{Game} owns board state only; \texttt{MinesweeperEnv}
  turns it into an MDP step and is the only component the agent talks
  to. Rendering is a passive observer, never a dependency of training.
\end{frame}

\begin{frame}{Flood-Fill Uncovering}
  \twocol{
    \centering
    \textbf{Before} --- click $(2,2)$\\[0.15cm]
    \begin{tikzpicture}[xscale=0.65, yscale=0.65]
      \foreach \x in {0,...,4} {
        \foreach \y in {0,...,4} {
          \msc[minimum size=0.62cm]{\x}{-\y}{mshidden}{}
        }
      }
      \msc[minimum size=0.62cm, draw=badred, ultra thick]{2}{-2}{mshidden}{}
    \end{tikzpicture}
  }{
    \centering
    \textbf{After} --- cascade complete\\[0.15cm]
    \begin{tikzpicture}[xscale=0.65, yscale=0.65]
      % zero-valued interior + safely revealed border (all \msnum{1})
      \msc[minimum size=0.62cm]{0}{0}{msrevealed}{}
      \msc[minimum size=0.62cm]{1}{0}{msrevealed}{}
      \msc[minimum size=0.62cm]{2}{0}{msrevealed}{}
      \msc[minimum size=0.62cm]{3}{0}{msrevealed}{\msnum{1}}
      \msc[minimum size=0.62cm]{4}{0}{mshidden}{}
      \msc[minimum size=0.62cm]{0}{-1}{msrevealed}{}
      \msc[minimum size=0.62cm]{1}{-1}{msrevealed}{}
      \msc[minimum size=0.62cm]{2}{-1}{msrevealed}{}
      \msc[minimum size=0.62cm]{3}{-1}{msrevealed}{\msnum{1}}
      \msc[minimum size=0.62cm]{4}{-1}{msrevealed}{\msnum{1}}
      \msc[minimum size=0.62cm]{0}{-2}{msrevealed}{}
      \msc[minimum size=0.62cm]{1}{-2}{msrevealed}{}
      \msc[minimum size=0.62cm]{2}{-2}{msrevealed}{}
      \msc[minimum size=0.62cm]{3}{-2}{msrevealed}{}
      \msc[minimum size=0.62cm]{4}{-2}{msrevealed}{}
      \msc[minimum size=0.62cm]{0}{-3}{msrevealed}{}
      \msc[minimum size=0.62cm]{1}{-3}{msrevealed}{}
      \msc[minimum size=0.62cm]{2}{-3}{msrevealed}{}
      \msc[minimum size=0.62cm]{3}{-3}{msrevealed}{}
      \msc[minimum size=0.62cm]{4}{-3}{msrevealed}{}
      \msc[minimum size=0.62cm]{0}{-4}{msrevealed}{}
      \msc[minimum size=0.62cm]{1}{-4}{msrevealed}{}
      \msc[minimum size=0.62cm]{2}{-4}{msrevealed}{}
      \msc[minimum size=0.62cm]{3}{-4}{msrevealed}{}
      \msc[minimum size=0.62cm]{4}{-4}{msrevealed}{}
    \end{tikzpicture}
  }
  \vspace{0.3cm}
  \small A stack-based traversal expands from the clicked \texttt{0} cell
  to every connected empty cell, revealing their numbered border in one
  step. The top-right cell stays hidden --- its three \msnum{1} neighbors
  are the only clue that it is a mine.
\end{frame}

\begin{frame}{Design Choices for Stable Training}
  \begin{itemize}
    \item \textbf{Safe first move}: if the first action hits a mine, the
      board is regenerated from the same RNG (not reseeded) until the
      first move is safe — avoids penalizing an unavoidable loss and
      keeps the training signal stable.
    \item \textbf{Mines never revealed to the agent}: even at a losing
      terminal step, the mine stays $-1$ only in the internal board; the
      \texttt{player\_board} keeps it as $-2$. Loss is communicated
      through the terminal reward alone, keeping the observation
      alphabet at 10 symbols (consistent with the planned one-hot
      encoding).
    \item \textbf{Guess detection}: a safe move is a guess if \emph{all}
      its neighbors are still unrevealed — used only for reward shaping,
      not for masking actions.
  \end{itemize}
\end{frame}
