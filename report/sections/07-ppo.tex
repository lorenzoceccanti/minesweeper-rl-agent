% !TeX root = ../slides.tex
% 07-ppo.tex — PPO agent, matching agents/ppo_agent.py.

\section{PPO Agent}

\begin{frame}{Proximal Policy Optimization \parencite{schulman2017ppo}}
  \twocol{
    \textbf{Components} (\texttt{agents/ppo\_agent.py})
    \begin{itemize}
      \item on-policy \alert{rollout buffer}: collect $1024$ steps, then
        drain it into $10$ update epochs of $256$-sample minibatches;
      \item separate \textbf{actor} and \textbf{critic} networks, separate
        Adam optimizers ($\text{lr}_{\text{actor}}=2{\times}10^{-4}$,
        $\text{lr}_{\text{critic}}=3{\times}10^{-3}$);
      \item independent gradient-clip norms: actor $0.5$, critic $5.0$;
      \item action masking on the actor logits before the categorical
        distribution is built (train \emph{and} test).
    \end{itemize}
  }{
    \textbf{Why on-policy?}
    \begin{itemize}
      \item no replay buffer bias — every update uses data collected
        under (close to) the current policy;
      \item the clipped objective bounds how far one update can move
        $\pi_\theta$ from the data-collecting policy, making on-policy
        reuse across $10$ epochs stable.
    \end{itemize}
  }
\end{frame}

\begin{frame}{Generalized Advantage Estimation \parencite{schulman2016gae}}
  \[
    \delta_t = r_t + \gamma\,(1-d_t)\,V(s_{t+1}) - V(s_t)
    \qquad
    A_t = \delta_t + \gamma\lambda\,(1-d_t)\,A_{t+1}
  \]
  \[
    \text{value target}_t = V(s_t) + A_t
  \]
  \begin{itemize}
    \item $\gamma = 0.95$, $\lambda = 0.95$ — computed backwards over the
      $1024$-step rollout;
    \item advantages are \alert{normalized} (zero mean, unit std) per
      minibatch before use, standard PPO variance-reduction trick;
    \item bootstraps are cut at terminal transitions ($d_t=1$), same as DQN.
  \end{itemize}
\end{frame}

\begin{frame}{Clipped Surrogate Objective}
  \[
    r_t(\theta) = \exp\!\big(\log \pi_\theta(a_t\mid s_t) - \log \pi_{\theta_{\text{old}}}(a_t\mid s_t)\big)
  \]
  \[
    \mathcal{L}^{\text{CLIP}}(\theta) = \mathbb{E}_t\Big[\min\big(r_t(\theta)A_t,\ \text{clip}(r_t(\theta), 1{-}\epsilon, 1{+}\epsilon)A_t\big)\Big]
  \]
  \[
    \mathcal{L}^{\text{actor}} = -\mathcal{L}^{\text{CLIP}} - c_{\text{ent}}\,\mathcal{H}[\pi_\theta]
    \qquad
    \mathcal{L}^{\text{critic}} = \big(V_\phi(s_t) - \text{value target}_t\big)^2
  \]
  \begin{itemize}
    \item $\epsilon = 0.1$ clip range — conservative, keeps policy updates
      small on a discrete action space of size $H{\cdot}W$;
    \item entropy bonus $c_{\text{ent}} = 0.005$ discourages premature
      convergence to a deterministic (over-exploiting) policy;
    \item actor and critic losses are backpropagated and clipped
      \alert{independently} through their own optimizers.
  \end{itemize}
\end{frame}

\begin{frame}{PPO Training Loop}
  \centering
  \begin{tikzpicture}[node distance=0.7cm and 1.4cm,
      component/.append style={minimum width=3.6cm, font=\small}]
    \node[component] (collect) {Collect $1024$ steps\\masked $\pi_\theta$\\store $(s,a,\log\pi,r,V,d)$};
    \node[component, right=of collect] (gae) {Compute GAE\\$A_t$, value targets};
    \node[component, below=of gae, fill=goodgreen!20] (clear) {Clear buffer\\$\theta_{\text{old}} \gets \theta$};
    \node[component, below=of collect] (update) {$10\times$ epochs\\minibatch $256$\\actor + critic loss};
    \draw[flowarrow] (collect) -- (gae);
    \draw[flowarrow] (gae) -- (clear);
    \draw[flowarrow] (clear) -- (update);
    \draw[flowarrow] (update) -- (collect);
  \end{tikzpicture}

  \vspace{0.3cm}
  \begin{minipage}{0.85\textwidth}
    \centering
    Repeated until the training episode budget ($150{,}000$ episodes for
    the headline run) is exhausted; periodic validation rollouts (masked,
    greedy) pick the checkpoint with the best win rate.
  \end{minipage}
\end{frame}
